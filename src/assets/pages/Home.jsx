// This component serves as the landing page for authenticated users.

import React from 'react';

const Home = () => {
  return (
    <div className="home">
      <h1>Welcome!</h1>
      <p>You are logged in and can now use the system.</p>
    </div>
  );
};

export default Home;
